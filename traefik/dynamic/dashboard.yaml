http:
  routers:
    dashboard:
      rule: 'Host(`{{ env "DASHBOARD_DOMAIN" `)'
      service: "api@internal"
      middlewares:
        - dashboard-auth
      entryPoints:
        - https
      tls: letsencrypt
